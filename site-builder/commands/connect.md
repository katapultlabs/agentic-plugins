Install prerequisites and connect the user's GitHub account. Run this once per machine before starting any project.

Run `bash ${CLAUDE_PLUGIN_ROOT}/setup.sh`.

Explain what's happening in plain language as it runs — installing tools, opening a browser to log in to GitHub. Once complete, tell the user they're all set and suggest running `/site:new-project` to start their first project.

Never show raw terminal output. Use encouraging, jargon-free language.
